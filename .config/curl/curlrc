# Follow redirects and set the referer automatically
location
referer = ";auto"

# Silent mode, but show errors
silent
show-error

# Fail on server errors and display the response body
fail-with-body

# Timeout if connection takes more than 5 seconds
connect-timeout = 5

